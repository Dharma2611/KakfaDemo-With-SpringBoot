Kafka


step 1 Start the kafka  with this command.
      :  bin\windows\kafka-server-start.bat config\server.properties
	  seen the message   like that KafkaServer started Transitioning from RECOVERY to RUNNING
	  default port of kafka server  advertised.listeners = PLAINTEXT://localhost:9092,CONTROLLER://localhost:9093
	  
	  
	  now create 1 topic using this command 
	     :   bin\windows\kafka-topics.bat --create --topic myfirst-topic --bootstrap-server localhost:9092 --partitions 1 --replication-factor 1
	now create 2 topice same comand with differant topicname
	
 after that we check how many topic register check all topice 
    :  bin\windows\kafka-topics.bat --bootstrap-server localhost:9092 --list
	
	now check decribe prticulalar topic so we use this command.
	bin\windows\kafka-topics.bat --bootstrap-server localhost:9092 --describe --topic myfirst-topic
	
	
	
